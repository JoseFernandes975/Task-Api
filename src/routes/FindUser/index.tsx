import SearchBar from '../../components/SearchBar';
import './styles.css';

export default function FindUser(){
 
   
    return(
        <main>
            <section className='jf-container'>
             <SearchBar />
            </section>
        </main>

    );
}